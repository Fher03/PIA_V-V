<h1>Products</h1>

<h2>
  <%= link_to "Create New Product", products_new_path %>
</h2>

<div id="products">
  <% @products.each do |product| %>
    <a href="/products/<%= product.id %>">
      <%= product.name %>
    </a>
  <% end %>
</div>